import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  inputer: {
    width: "100%",
    backgroundColor: "#E5BF3C",
    padding: 10,
    borderRadius: 10,
    color: "#000",
    fontSize: 20,
    borderWidth: 1,
    textAlign: "center",
  },
});
